import 'package:flutter/material.dart';

import 'const.dart';

class WaterScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: kBackgroundColor,
      body: Center(
        child: Text('Water Reminder here'),
      ),
    );
  }
}
